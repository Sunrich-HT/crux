先把问题压缩成“竞争解释的预测”，暂时不要讨论 identity shortcut 的机制。

论文给出的直接观测是：

- Fig. 4 左：在相同训练流程下，plain-34 的训练误差几乎全程高于 plain-18，验证误差也更高。
- 作者报告把训练迭代延长到 3 倍仍有 degradation。
- BN 下前向信号方差非零，反向梯度范数也正常。这只是作者额外检查的结果，不是曲线本身显示的。
- Fig. 4 右：加入无额外参数的 shortcut 后，ResNet-34 的训练误差低于 ResNet-18；CIFAR-10 的 Fig. 6 重复了深度趋势反转。
- 相比之下，ResNet-1202 与 ResNet-110 训练误差相近、测试误差更高，作者才把这一处归因于过拟合。

现在请你只完成最早的推理环节：

设两个解释：

1. \(H_{\text{overfit}}\)：更深模型把训练集拟合得至少一样好，但泛化更差。
2. \(H_{\text{protocol-opt}}\)：在论文给定的初始化、BN、SGD、学习率和训练预算下，更深 plain 参数化更难达到低训练误差。

根据 Fig. 4 左图的细线，分别判断哪个解释被反驳、哪个解释得到支持；务必把“得到支持”写成对这套训练协议的条件性结论，而不是关于所有优化器或无限训练时间的普遍结论。先用两句话作答。identity shortcut 是否证明机制，要等这个“现象层与归因层”的边界立稳后再判断。

原文见 [resnet.pdf](/private/tmp/crux-forward-eval/resnet.pdf)。