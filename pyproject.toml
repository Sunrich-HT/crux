[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "crux-supervisor"
version = "0.2.1"
description = "A deterministic disclosure-policy core for evidence-governed thinking agents"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Crux contributors" }]
keywords = ["ai", "evidence-governed", "research", "decision-support", "agent-skill"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Sunrich-HT/crux"
Documentation = "https://github.com/Sunrich-HT/crux#readme"
Repository = "https://github.com/Sunrich-HT/crux.git"
Issues = "https://github.com/Sunrich-HT/crux/issues"

[project.scripts]
crux = "crux_supervisor.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/crux_supervisor"]
